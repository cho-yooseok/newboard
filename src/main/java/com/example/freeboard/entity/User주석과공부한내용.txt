정리
User 엔티티
DB의 users 테이블과 매핑
Spring Security의 UserDetails를 구현하여 로그인/인증 시 사용

보안 관련
getAuthorities() : 사용자의 권한을 ROLE_권한명 형태로 반환
isAccountNonExpired() 등 : 계정 상태 관리 (현재는 전부 true → 제약 없음)

비밀번호 관리
setEncodedPassword() : 평문 비밀번호를 PasswordEncoder로 암호화 후 저장

Auditing
createdAt : 사용자 계정 생성 시간을 자동 기록


@Override
재정의

여기서는 UserDetails 인터페이스에 정의된 getPassword() 메서드를 정의하고 있다.

public String getPassword()

public 접근제어자
String 반환타입 String 문자열을 반환한다는 의미
getPassword()
메서드 이름
사용자 비밀번호를 가져오는 메서드


@Override
public boolean isAccountNonExpired() {
    return true;        // 계정 만료 여부 (true : 만료되지 않음)
}

